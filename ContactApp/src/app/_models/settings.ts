export interface ISettings {
    settingsId: number,
    chartTitleFontSize: number;
    chartScaleLabelFontSize: number;
    chartLineWidth: number;
    chartLineColour: string;
    chartAreaBackgroundColour: string;
    chartModalBackgroundColour: string;
}